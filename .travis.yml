sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t salvosk/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true salvosk/docker-react npm run test -- --coverage
